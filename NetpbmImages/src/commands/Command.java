package commands;
/**
 * Представя базова команда в приложението.
 * Всички команди имплементират този интерфейс.
 */
public interface Command {
    /**
     * Изпълнява логиката на командата.
     */
    String execute();
}
